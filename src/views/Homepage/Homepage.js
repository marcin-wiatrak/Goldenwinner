import React from 'react';
import PropTypes from 'prop-types';

const Homepage = (props) => (
    <div>
        <h1>Panel logowania / strona główna</h1>
    </div>
);

Homepage.propTypes = {};

export default Homepage;
